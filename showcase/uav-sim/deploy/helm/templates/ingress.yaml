{{- if and .Values.interactive.enabled .Values.liveView.streamIngress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: uav-sim-live-stream
  labels:
    {{- include "uav-sim.labels" . | nindent 4 }}
  {{- with .Values.liveView.streamIngress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.liveView.streamIngress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.liveView.streamIngress.tls.enabled }}
  tls:
    - hosts:
        - {{ required "liveView.streamIngress.host is required" .Values.liveView.streamIngress.host | quote }}
      secretName: {{ required "liveView.streamIngress.tls.secretName is required" .Values.liveView.streamIngress.tls.secretName }}
  {{- end }}
  rules:
    - host: {{ required "liveView.streamIngress.host is required" .Values.liveView.streamIngress.host | quote }}
      http:
        paths:
          - path: /uav-sim/live
            pathType: Prefix
            backend:
              service:
                name: uav-sim-mcp
                port:
                  number: {{ .Values.liveView.streamGatePort }}
{{- end }}
