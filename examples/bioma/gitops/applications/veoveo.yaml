apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bioma-veoveo
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: bioma
  sources:
    - repoURL: charts-registry.argocd.svc.cluster.local/charts
      chart: veoveo
      targetRevision: 0.1.0-258665920cfd
      helm:
        releaseName: veoveo
        valueFiles:
          - $configuration/examples/bioma/values.yaml
          - $configuration/examples/bioma/k3d-values.yaml
          - $configuration/examples/bioma/images.lock.yaml
    - repoURL: https://github.com/BiomaAI/veoveo.git
      targetRevision: fb96bb790c665468e2f1eeac3566cb8b7baa012b
      ref: configuration
  destination:
    server: https://kubernetes.default.svc
    namespace: veoveo
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
